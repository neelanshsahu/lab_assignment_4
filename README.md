# lab_assignment_4# JavaScript Quiz Game (MCQ + Score + High Score)

This is a simple JavaScript quiz game that runs completely in the browser console.  
The quiz contains multiple-choice questions, checks the user’s answers, shows feedback,  
and also stores the highest score using `localStorage`.

---

## 📌 Features

- Multiple-choice questions (A, B, C, D)
- Score tracking
- Shows correct answer after each question
- High score saved in browser using `localStorage`
- Clean and beginner-friendly JavaScript code
- Runs without any HTML or CSS

---

## 📂 How It Works

1. The quiz questions are stored inside an array as objects.  
2. The `runQuiz()` function goes through each question using a loop.  
3. User gives answers using `prompt()`.  
4. Answers are checked with simple conditions.  
5. Score is calculated and displayed at the end.  
6. High score is saved and compared on every quiz attempt.

---

## ▶️ How to Run

1. Open **any web browser** (Chrome recommended).  
2. Right-click → **Inspect** → open the **Console**.  
3. Copy and paste the entire JavaScript code into the console.  
4. Press **Enter**.  
5. The quiz will start immediately.

---

## 🧠 Concepts Used

- Arrays  
- Objects  
- For loops  
- Functions  
- Conditionals (`if-else`)  
- String methods (`trim()`, `toLowerCase()`)  
- Browser alerts and prompts  
- `localStorage` for saving high scores  

---



